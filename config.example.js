/*
 *  Configurations for dust-on-me
 *                 ⓒ SD SkyKlouD
 */

// Rename this file to `config.js` when you are done with filling these fields.

module.exports = {
    // Twitter API configurations (for module `twit`)
    twitterConfigs: {
        consumer_key:        "",    // Consumer Key
        consumer_secret:     "",    // Consumer Secret
        access_token:        "",    // Access Token
        access_token_secret: ""     // Access Token Secret
    },

    // Airkorea UTF-8 URL encoded API Key (data.go.kr / openapi.airkorea.or.kr)
    airKoreaApiKey: "",

    // Enable/disable debugging log (value must be `true` or `false`)
    debuggingLog: false,

    // Screen name of the bot (without `@`)
    screenName: "",

    // Twitter account ID(string contains only numeric characters) of the bot
    botAccountId: "0",
    
    // Twitter account ID(string contains only numeric characters) of the bot maintainer
    maintainerAccountId: "0"
};